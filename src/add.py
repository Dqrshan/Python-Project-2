def add(xid, xname, xprice, er):
    id = xid.get()
    name = xname.get()
    price = xprice.get()
    if not id or not name or not price:
        er.showerror('Error', 'Missing Fields')
    else:
        try:

        except:
